# Technology Stack

> Generated by /map on 2026-04-30

## Runtime

| Technology | Version | Purpose |
|------------|---------|---------|
| Python | 3.11+ (recommended) | Backend daemon — NLP, AI, monitoring, scheduler, TTS, macros |
| Rust | (Tauri version TBD) | Native desktop shell (Tauri framework) |
| TypeScript / React | (TBD) | Tauri frontend UI (tray, overlays, dashboard) |
| SQLite | 3.x (WAL mode) | Local persistent storage — all user data |

> ⚠️ **No codebase exists yet.** The stack is inferred from design artifacts (SQL schema, component diagram, voice grammar document). No `package.json`, `requirements.txt`, or `Cargo.toml` is present in the repository.

---

## Backend — Python (Planned)

| Package | Version | Purpose |
|---------|---------|---------|
| `anthropic` | latest | Claude API client for AI reasoning and generation |
| `openai-whisper` | latest | Local speech-to-text transcription |
| `sounddevice` or `pyaudio` | latest | Microphone capture for voice pipeline |
| `pyttsx3` | latest | Default offline TTS engine |
| `elevenlabs` | latest (optional) | Premium cloud TTS engine |
| `coqui-tts` | latest (optional) | Offline premium TTS alternative |
| `apscheduler` | 3.x | Job scheduling (reminders, briefings, timers) |
| `websockets` or `flask` | latest | Local WebSocket server for browser extension |
| `icalendar` | latest | Read/write `.ics` calendar files |
| `pyyaml` | latest | Parse `config.yaml` and `workspaces.yaml` |
| `fuzzywuzzy` or `rapidfuzz` | latest | Fuzzy task/project name matching for slot resolution |
| `sqlite3` | stdlib | SQLite database driver (built into Python) |

## Backend — Development Dependencies (Planned)

| Package | Version | Purpose |
|---------|---------|---------|
| `pytest` | latest | Unit tests for NLP parser, action dispatcher, scheduler |
| `pytest-asyncio` | latest | Async test support |
| `black` | latest | Code formatting |
| `ruff` | latest | Linting |
| `mypy` | latest | Static type checking |

---

## Frontend — Tauri + TypeScript (Planned)

| Package | Version | Purpose |
|---------|---------|---------|
| `@tauri-apps/api` | 2.x | Tauri JS bindings (IPC, window management, tray) |
| React or Svelte | TBD | UI framework for dashboard, overlays, settings |
| TypeScript | 5.x | Type-safe frontend code |
| Tailwind CSS or Vanilla CSS | TBD | Styling |
| Recharts or Chart.js | TBD | Analytics charts (deep work, distraction breakdowns) |

> ⚠️ **Frontend framework not yet decided.** Tauri is confirmed by the component diagram (`Tauri UI Shell`). React/TS is referenced in Quantum Studio GSD phases present in the repo but those are from a different project.

---

## Infrastructure

| Service | Provider | Purpose |
|---------|----------|---------|
| Claude API | Anthropic (cloud) | Reasoning, PRD generation, briefings, rubber duck |
| ElevenLabs | ElevenLabs (cloud, optional) | Premium TTS voice |
| SQLite database | Local filesystem | All persistent user data |
| Whisper STT | Local (OpenAI model weights) | Speech transcription |
| APScheduler | Local (Python process) | Reminder and briefing scheduling |
| Browser Extension | Local WebSocket | Tab and dwell tracking |
| System OS APIs | Windows/macOS | Window detection, app launch, DND |

---

## Design Artifact Tools

| Tool | Purpose |
|------|---------|
| `docx` (npm) | Used by `gen_grammar.js` to generate the Voice Grammar DOCX document |
| `fs` (Node stdlib) | File writing in `gen_grammar.js` |
| Node.js | Runtime for `gen_grammar.js` document generator |

> These are **document generation utilities only**, not part of the ZENO runtime.

---

## Configuration

| Variable / Key | Location | Purpose | Required |
|----------------|----------|---------|----------|
| `claude_model` | `user_profile` table | Which Claude model to use (default: `claude-sonnet-4`) | Yes |
| `tts_engine` | `user_profile` table | TTS provider: `pyttsx3` / `elevenlabs` / `coqui` | Yes |
| `stt_model` | `user_profile` table | Whisper model size (default: `whisper-base`) | Yes |
| `wake_word` | `user_profile` table | Primary activation phrase (default: `Hey Zeno`) | Yes |
| `working_hours_start/end` | `user_profile` table | Working hour boundaries for scheduling | Yes |
| `timezone` | `user_profile` table | User timezone for date resolution | Yes |
| `ANTHROPIC_API_KEY` | Environment variable | Anthropic Claude API authentication | Yes |
| `ELEVENLABS_API_KEY` | Environment variable | ElevenLabs TTS API authentication | If ElevenLabs TTS used |
| `workspaces.yaml` | `~/Zeno/workspaces.yaml` | Workspace macro definitions (YAML) | No (optional workspaces) |
| `config.yaml` | `~/Zeno/config.yaml` | Human-editable config file (mirrors `user_profile`) | No (DB is source of truth) |

---

## Database Details

| Aspect | Value |
|--------|-------|
| Engine | SQLite 3.x |
| File path | `~/Zeno/Zeno.db` |
| Journal mode | WAL (Write-Ahead Logging) |
| Foreign keys | ON |
| Synchronous | NORMAL |
| Temp store | MEMORY |
| Memory map | 256 MB |
| Schema version | 1.0 |
| Tables | 22 concrete tables + 1 virtual (FTS5) |
| Views | 4 convenience views |
| Triggers | 7 auto-maintenance triggers |
| Indexes | 25+ covering all high-query columns |

---

## Voice Grammar Specification

| Aspect | Value |
|--------|-------|
| Categories | 10 intent categories |
| Named intents | 52 intents |
| Slot types | 31 typed slots |
| Dialogue flows | 5 multi-turn conversation state machines |
| Confidence threshold | 0.75 (below triggers clarification) |
| NLP pipeline | STT → Normalisation → Intent Classification → Slot Extraction |
| Output format | Structured JSON intent object |
| Document | `Zeno_VoiceGrammar.docx` (generated by `gen_grammar.js`) |
